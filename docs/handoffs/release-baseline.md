# Campaign continuation baseline

The published story contains 154 reviewed scenes across seven stages, with 27–43-choice full journeys and earlier absorbing endings. Version 2.0.0-campaign.1 is pinned by content hash `33630493964e3613acaa98e9a825a1698059805f2a73de0cf7bea47dc96bdde3`. [Deployment record](../operations/deployment-record.md) and [release evidence](../../reports/validation/full-campaign-release.md) identify the verified public version and actual checks.

Campaign-only publication is the standing scope. Keep audit, mockups and their local notes out of Git and Pages. The build rejects a reports folder in its output. Do not override the ignore rules to make review links public.

Next work: integrate a selected visual direction while preserving the tested semantic routes/input/save/reduced modes; extend coarse institution/evaluation/service semantics only with explicit contracts and fixtures; test physical devices, screen readers, 400% zoom, context loss and long-run memory; establish human pacing without inferring it from automated routes. The control board, conversation, wire, art assets and content remain separate modules.

The optional collector is built but not publicly activated. Its remote schema, approved manifests, operational withdrawal, retention/logging and backup behavior need actual verification before enabling frontend sharing. No formal study or participant-efficacy claim is active. Synthetic replay witnesses are software evidence and must never become public response statistics.

Preserve all four archived slice bundles. Never relabel an old save or a diagnostic draft as the current campaign. Use the existing content inspector, headless seeded simulations, exact exported replay and actual browser evidence together.
