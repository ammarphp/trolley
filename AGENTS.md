# trolley. v2

Read `docs/plan/ACCEPTED_PLAN.md`, `docs/handoffs/NEXT_ACTIONS.md`, and your task reservation before edits. The original brief/addendum and execution handoff live in `beta version/ai_safety_trolley_codex_handoff/`.

Keep the pure deterministic simulation separate from UI, audio, time, persistence and telemetry. No real LLM calls, arbitrary executable content, unseeded simulation randomness, pre-consent uploads, fake public statistics, or source PDFs in public artifacts. Preserve v1 exports and withdrawal access. Research claims require exact inspected sources and evidence boundaries. Do not call drafts, placeholders or unverified gates complete.

The lead alone edits contracts, package/lock files, shared registries, database migrations and workflow integration. Other agents use exact non-overlapping reservations in the task ledger. Test the behavior affected, provide evidence and report limitations. Keep all real pause/settings/privacy controls truthful and operational during fictional horror.
